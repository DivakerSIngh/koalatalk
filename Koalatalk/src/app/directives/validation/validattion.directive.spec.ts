import { ValidattionDirective } from './validattion.directive';

describe('ValidattionDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidattionDirective();
    expect(directive).toBeTruthy();
  });
});
